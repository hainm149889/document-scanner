///
/// ExtractedDocumentData.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/**
 * Represents an instance of `ExtractedDocumentData`, backed by a C++ struct.
 */
public typealias ExtractedDocumentData = margelo.nitro.rndocumentscanner.ExtractedDocumentData

public extension ExtractedDocumentData {
  private typealias bridge = margelo.nitro.rndocumentscanner.bridge.swift

  /**
   * Create a new instance of `ExtractedDocumentData`.
   */
  init(idNumber: String, fullName: String, dateOfBirth: String, gender: String, nationality: String, placeOfOrigin: String, placeOfResidence: String, expiryDate: String, issueDate: String, mrzLines: [String], rawText: [String]) {
    self.init(std.string(idNumber), std.string(fullName), std.string(dateOfBirth), std.string(gender), std.string(nationality), std.string(placeOfOrigin), std.string(placeOfResidence), std.string(expiryDate), std.string(issueDate), { () -> bridge.std__vector_std__string_ in
      var __vector = bridge.create_std__vector_std__string_(mrzLines.count)
      for __item in mrzLines {
        __vector.push_back(std.string(__item))
      }
      return __vector
    }(), { () -> bridge.std__vector_std__string_ in
      var __vector = bridge.create_std__vector_std__string_(rawText.count)
      for __item in rawText {
        __vector.push_back(std.string(__item))
      }
      return __vector
    }())
  }

  @inline(__always)
  var idNumber: String {
    return String(self.__idNumber)
  }
  
  @inline(__always)
  var fullName: String {
    return String(self.__fullName)
  }
  
  @inline(__always)
  var dateOfBirth: String {
    return String(self.__dateOfBirth)
  }
  
  @inline(__always)
  var gender: String {
    return String(self.__gender)
  }
  
  @inline(__always)
  var nationality: String {
    return String(self.__nationality)
  }
  
  @inline(__always)
  var placeOfOrigin: String {
    return String(self.__placeOfOrigin)
  }
  
  @inline(__always)
  var placeOfResidence: String {
    return String(self.__placeOfResidence)
  }
  
  @inline(__always)
  var expiryDate: String {
    return String(self.__expiryDate)
  }
  
  @inline(__always)
  var issueDate: String {
    return String(self.__issueDate)
  }
  
  @inline(__always)
  var mrzLines: [String] {
    return self.__mrzLines.map({ __item in String(__item) })
  }
  
  @inline(__always)
  var rawText: [String] {
    return self.__rawText.map({ __item in String(__item) })
  }
}
